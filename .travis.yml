language: node_js
node_js:
  - "5.5"
  - "4.2"
python:
  - "2.7"
before_script:
  - npm install -g npm
  - npm install -g gulp
install:
  - pip install cookiecutter
  - cookiecutter . --no-input
  - cd angular2-seed
  - npm install
script:
  - npm test
sudo: false
